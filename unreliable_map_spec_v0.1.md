# Unreliable Map — v0.1 設計書（AIコーディングエージェント向け）

> **仮称:** Unreliable Map（あてにならない地図）
> **対象:** このドキュメントは、MVP（v0.1）を実装するAIコーディングエージェントへの指示書です。
> **原則:** 迷った時は「設計憲法」（§2）を最優先で参照すること。憲法に反する最適化・省略はしない。

---

## 1. 目的と、v0.1で証明すべきこと

これは「マップを埋めるゲーム」ではない。**不完全な地図・曖昧な記録・信頼度の異なる情報**を頼りに、それを信じるか疑うかを、プレイヤー自身の責任で選ぶテキストローグである。

v0.1は製品ではなく**検証プロトタイプ**。証明すべき問いはただ一つ。

> **主問:** 同じ「性格」を持つが毎回少し違う迷宮に対して、過去の曖昧な記録を読みながら潜るのは面白いか？

そして、この問いの裏には**隠れた主題**がある。実装者はこちらを常に意識すること。

> **裏主題:** プレイヤーは「言葉（危険・怪しい 等）」を「確率」に翻訳する“体内変換表”を、プレイを通じて自力で作れるか？

v0.1の設計判断は、すべてこの2点に貢献するかどうかで決める。貢献しない要素（ランキング、共有、美麗UI、課金、LLM生成）は**すべて後回し**（§14）。

---

## 2. 設計憲法（変更不可の原則）

以下は仕様の骨格であり、実装上の都合で曲げてはならない。

1. **プレイヤーを騙さない。信頼度の異なる情報を与えるだけである。**
2. **自分の観測は正しい。** 実際に踏破した地形は100%正確。視界で見た地形はほぼ正確。ここがゲームへの信頼の土台。
3. **外れるのは「記録・噂・推測」だけ。** 古地図・生還者メモ・気配・古い自分のログは外れることがある。ただし——
4. **外れ方には必ず理由がある。** 完全な理不尽は最小化し、「読めば予測できたかもしれない」と感じさせる（§8）。
5. **UIに数字を出さない。** 内部確率は持つが、プレイヤーには言葉（ラベル）でしか見せない。翻訳はプレイヤーにさせる。
6. **ラベルは内部確率の正直な射影である。** ラベルは情報を圧縮するが、嘘はつかない。同じ内部確率帯は常に同じラベルになる（§6）。だから翻訳表が学習可能になる。
7. **正本はプログラム、LLMは語り部。** 勝敗・入手物・リスク判定・確率・ダンジョン生成は**すべてプログラムが決定論的に決める**。LLM（v0.1では未使用）は描写・噂文・記録文の生成のみに限定し、ゲーム状態を書き換えさせない。

---

## 3. スコープ（v0.1 の in / out）

| 機能 | v0.1 |
|---|---|
| 完全ソロプレイ（1プレイ＝1回の潜行） | **必須** |
| 迷宮の「性格」による生成 | **必須** |
| 3層マップ（踏破 / 推測 / 古地図） | **必須** |
| 信頼度ラベル（§6） | **必須** |
| 危険度ラベル（§7） | **必須** |
| 外れ方の段階制御（§8） | **必須** |
| 行動前の「判断材料」表示 → 行動後の「結果」対応表示（§10） | **必須** |
| 死亡ログ（判断文脈の記録） | **必須** |
| 計測フック（開発者向け、非表示）（§12） | **必須** |
| ブラウザで1潜行が最後まで回る | **必須** |
| 地図共有 / QR / リンク | 後回し |
| ランキング / 名迷宮 / 生還率 | 後回し |
| 実プレイヤー間の共有 | 後回し |
| LLMによる描写・噂・助言生成 | 後回し（v0.1はテンプレ文で代替） |
| iPhoneアプリ化 / PWA化 | 後回し |
| セーブ | v0.1は不要（1潜行完結でよい。任意で自動保存） |

---

## 4. アーキテクチャ

### 4.1 推奨スタックと構成

- **推奨: ブラウザ（TypeScript）。** v0.1の目的は「一人で遊んで面白いか」の即時プレイテスト。ブラウザなら配布・共有・反復が最速で、後の共有/PWA化とも地続き。
- ただし**コアはUI非依存**にする。Pythonで書いても移植できる純ロジックとして設計すること（実装者がPythonを選ぶ場合も、下記のデータモデル・アルゴリズムは同一）。
- 構成は最低限、次の2層に分ける。
  - **core（純ロジック）:** ダンジョン生成・状態・確率解決・判定。UIもDOMも一切触らない。副作用は注入されたPRNGのみ。
  - **renderer（テキストUI）:** coreの状態を読んで画面テキストを組み立て、プレイヤーの選択をcoreに渡すだけ。

```
/src
  /core
    rng.ts            # 単一のシード付きPRNG（下記4.2）
    character.ts      # DungeonCharacter（迷宮の性格）
    generate.ts       # 性格＋シード → DungeonInstance
    hearsay.ts        # 古地図・噂の生成（別インスタンスの劣化サンプル）§9.3
    confidence.ts     # 情報源→内部確率→ラベル §6
    danger.ts         # プレイヤー状態＋敵 → 危険度確率→ラベル §7
    resolve.ts        # 情報の当否解決・外れ方の選択 §8
    state.ts          # ゲーム状態・ターンループの遷移
    telemetry.ts      # 計測フック §12
  /ui
    render.ts         # 画面テキスト組み立て §11
    input.ts          # 行動選択
  main.ts
```

### 4.2 決定論とRNG規律（重要）

- **`Math.random()` を直接使うことを禁止する。** 乱数はすべて、シードから作った単一のPRNG（例: mulberry32 / xorshift）をcore全体に注入して使う。
- 理由: (a) 潜行を再現可能にしてデバッグ・的中率チューニング（§13）を可能にするため。(b) 「性格は同じだが毎回違う」を、シード切り替えだけで制御するため。
- **1潜行 = 1つの `runSeed`。** その潜行の地形・敵配置・アイテムは `runSeed` で完全に決まる（＝潜行中、自分の観測は決して揺れない。憲法2）。
- 新しい潜行は新しい `runSeed`。同じ性格でも別インスタンスになる。

---

## 5. 中心設計：「迷宮＝インスタンスではなく性格」

この企画が長く抱えてきた矛盾——**固定マップは一度完全記録されたら“解けて”終わる**——を、ここで構造的に解く。

- 共有・記憶される単位は**具体的な地図ではなく `DungeonCharacter`（性格＝生成制約セット）**。
- 例（「沈黙の井戸」の性格）:
  - 金属音（金属系の敵）が出やすい
  - 上層に毒罠が出やすい
  - 下層ほど帰還困難（帰り道が崩れやすい / 距離が伸びる）
  - 薬の効果が通常より不安定
  - 報酬は武器寄り
  - 敵は少ないが一体ごとの危険度が高い
- **具体地形は毎回、性格＋`runSeed` から生成する。** 統計的に似るが同一ではない。
- **したがって「古地図・噂」は、この性格の“別インスタンス”に由来する。** だから部分的に当たり、部分的にズレる——ゲームが嘘をつくのではなく、そもそも別の潜行の記録だから合わないだけ。これが憲法1（騙さない）と「わからなさ」の両立を成立させる。

> **実装上の含意:** 古地図の生成は「別シードで同じ性格の迷宮を生成し、それを劣化・部分化させたもの」として作る（§9.3）。これにより、古地図の予測と実地形の間に**本物の相関**が生まれ、その相関の強さを的中率テーブル（§6）に合わせて調整できる。

---

## 6. 信頼度システム

### 6.1 情報源 → 内部確率（基準値）

各情報は生成時に、その情報源に応じた**内部真偽確率 `p`（0.0–1.0）**を持つ。

| 情報源 | 内部確率 `p` の基準帯 |
|---|---|
| 自分が踏破した地形 | 1.00（確定） |
| 視界内で見た地形 | 0.95–1.00 |
| 音・匂い・気配 | 0.60–0.80 |
| 古い見取り図 | 0.40–0.80 |
| 生還者メモ | 0.30–0.70 |
| 死亡者メモ | 0.20–0.60 |
| 自分の過去ログ | 0.70–0.90 |
| AI的な助言・語り（v0.1未使用、枠だけ確保） | 0.50–0.75 |

- **下限を0にしない。** 低信頼でも `p` を0–0.2 に落とさない。落とすとプレイヤーが「どうせ嘘」と学習して情報を無視し、判断が消える（裏主題の死）。
- **上限も1にしない**（自分の観測を除く）。

### 6.2 内部確率 → 表示ラベル

`p` は次の帯で**ラベルに射影**する。UIにはラベルのみ表示。**この対応は全情報で共通・不変**（憲法6）。

| 表示ラベル | 内部確率 `p` | プレイヤー体感 |
|---|---|---|
| 確か | 0.95–1.00 | ほぼ信じてよい |
| かなり信じられる | 0.80–0.90 | 判断の軸になる |
| ありそう | 0.60–0.75 | 信じる価値はあるが外れる |
| 怪しい | 0.40–0.55 | 状況次第で使う |
| 噂程度 | 0.25–0.40 | 捨て情報ではないが危険 |

> **なぜ矛盾しないか:** ラベルは `p` の粗い量子化であって、`p` を歪めていない。だから「怪しい」は毎回だいたい半々で当たる。プレイヤーは体験を重ねて「怪しい＝五分」を体で覚えられる。これが翻訳表の学習可能性（裏主題）を担保する。

---

## 7. 危険度システム

敵・罠の危険度は、**プレイヤーの現在状態（装備・空腹・HP等）と対象の強さから、死亡/重傷確率を計算**し、次のラベルに射影する。数字は出さない。

| 表示ラベル | 内部の死亡・重傷リスク |
|---|---|
| なんとかなりそう | 0–10% |
| 油断はできない | 10–25% |
| 危険 | 25–45% |
| かなり危険 | 45–70% |
| 死の気配 | 70%以上 |

- 危険度は**状態依存**であること。同じ敵でも「革鎧が傷んでいる」「空腹」なら1段上がる。これが「装備・空腹も効く」という学習を生む（死亡ログで振り返らせる）。

---

## 8. 外れ方（miss patterns）

情報が「外れる」時、単なる当たり/外れの二値にしない。外れ方に段階を持たせ、v0.1では**理不尽度の低い外れ方を優先**する。

| 外れ方 | 例 | 理不尽度 | v0.1の重み |
|---|---|---|---|
| 位置が少しズレる | 泉は南東ではなく南西だった | 低 | 高 |
| 内容は合うが条件が違う | 泉はあるが枯れている | 中 | 高 |
| 古い情報だった | 前は通路だったが崩れている | 中 | 中 |
| 主観の誤認 | 「金属音」は敵ではなく罠だった | 中 | 中 |
| 完全な誤情報 | 何もなかった | 高 | **低（稀）** |

**解決アルゴリズム（`resolve.ts`）:**
1. 情報 `info` は内部確率 `p` を持つ（§6.1）。
2. PRNGで `roll < p` なら **hold（当たり）** → 正確に反映して描写。
3. `roll >= p` なら **miss** → 上表の重みで外れ方を1つ抽選。v0.1では「完全な誤情報」の重みを十分低く設定。
4. missでも**理由が残るように**描写する（古い/条件違い/誤認 等）。「なぜ外れたか」がログに残ること（§10・§8憲法4）。

---

## 9. マップ（3層）

### 9.1 3つの層

| 層 | 内容 | 信頼度 | 表示 |
|---|---|---|---|
| 踏破層 | 実際に歩いたマス | 確定（100%） | 確定タイルとして描画 |
| 推測層 | 視界・音・匂い・気配からの周辺推測 | 中 | `?` や気配テキストで曖昧に |
| 古地図層 | 別インスタンス由来の見取り図・噂 | 低〜中 | 信頼度ラベル付きで別枠表示 |

- **v0.1の絶対線引き:** 踏破層・視界は嘘をつかない。嘘をつくのは推測層と古地図層のみ（憲法2）。

### 9.2 テキストマップ描画

- 小さなASCIIマップを出す（プレイヤーは「テキストマップは欲しい／わからないからドキドキする」と明言）。
- 記号例:
  - `@` プレイヤー / `.` 踏破済みの床 / `#` 壁 / `?` 未確定の境界（＝判断を迫るマス）
  - 未探索は空白または闇。
- **`?` は単なる未踏マスではなく「境界」として機能させる。** その先へ進めば新情報、しかし罠/敵/空腹前進のリスク。ここが冒険感の核。

例（画面内のマップ枠）:
```
沈黙の井戸 B2F

  #######
  #.....#
  #..@..#
  #..?..#
  ###?###
```

### 9.3 古地図・噂の生成（`hearsay.ts`）

1. 現在の潜行とは**別のシード**で、同じ `DungeonCharacter` の迷宮を生成する（＝「過去の誰かが潜った別インスタンス」）。
2. それを**劣化・部分化**する: 一部フロアのみ / 位置ドリフト / 古い特徴（崩落前の通路など）。
3. 劣化の度合いから内部確率 `p` を導出し（§6.1の帯に収める）、ラベルを付す。
4. v0.1では実プレイヤーは存在しないので、生還者メモ・死亡者メモも同様に**手続き的に生成**する（テンプレ文でよい。LLMは後回し）。

> これにより、古地図は「性格は同じだが別の潜行」なので、当たったり外れたりが**原理的に正当**になる。憲法1を守ったまま「あてにならない地図」が成立する。

---

## 10. 学習装置：行動前 → 行動後の対応

**裏主題（翻訳表を作らせる）の心臓部。** すべての意味ある行動で、**判断材料（行動前）**と**結果（行動後）**を対応させて見せる。

**行動前（判断材料の提示）:**
```
南から金属音がする。（気配：ありそう）
古い見取り図では、この先に小部屋がある。（信頼度：怪しい）
状態：空腹ぎみ / 革鎧は傷んでいる / 松明は残り少ない
```

**行動後（結果と、事前情報への対応）:**
```
南には小部屋があった。（古地図：概ね当たり／一部は崩れていた）
金属音の正体は、壊れた鎧を引きずる敵だった。（気配：内容は概ね当たり）
```

**死亡ログ（判断文脈を必ず残す）:**
```
死亡記録：
あなたは「かなり危険」と記録された金属音の主に挑み、敗北した。
直前の判断材料：
  ・革鎧は傷んでいた
  ・空腹ぎみだった
  ・古い見取り図では、この先は小部屋とされていた（信頼度：怪しい）
  ・南から金属音（気配：ありそう）
```

- この対応が**毎回**あることで、プレイヤーは「かなり危険＝本当に危ない」「怪しい古地図だけで進むのはまずい」「空腹・装備劣化が効く」を自力で学ぶ。
- 死亡ログは懐古の飾りではなく、**曖昧な言葉を意思決定可能にする学習装置**として実装すること。

---

## 11. ゲームループ（1画面単位）

1潜行は次のターンループを繰り返す。1画面 = 現在地の描写 + マップ + 状態 + 行動選択肢。

```
[画面]
  タイトル: 迷宮名 + 階層
  マップ枠（3層）
  情景テキスト（方向ごとの気配・音・匂い、信頼度ラベル付き）
  古地図/噂枠（あれば、信頼度ラベル付き）
  状態枠（空腹 / 装備の傷み / 松明 / 体調 …数字なし、言葉で）
  行動枠: [進む(方向)] [耳を澄ます] [古い地図を見る] [休む] [戻る/降りる] …

[ターン処理]
  1. プレイヤーが行動を選ぶ
  2. core が結果を決定論的に解決（§8のresolve含む）
  3. マップ・状態を更新
  4. 「結果と事前情報の対応」を提示（§10）
  5. 死亡 or 脱出なら終了（死亡ログ / 生還記録）、そうでなければ次ターン
```

- v0.1の迷宮規模: **3〜5階層**程度で十分（10階は後回しでよい。ループの手触り検証が目的）。
- 状態要素（v0.1最小）: 空腹、装備の傷み、松明（光源）、HP相当の体調。すべて言葉で表示。

---

## 12. 計測フック（開発者向け・非表示）

**裏主題の検証と、的中率チューニングのために必須。プレイヤーには見せない。**

各潜行で、少なくとも以下を構造化ログ（JSON）に記録する:

- 各情報提示について: `{source, label, internal_p, predicted, actual, held: bool, miss_pattern?}`
- 各危険判断について: `{danger_label, internal_risk, engaged: bool, outcome}`
- プレイヤーの選択と結末（脱出/死亡、階層、ターン数）

用途:
1. **ラベルごとの実的中率**が意図した帯（§6.2 / §7）に入っているか測る。
2. プレイヤーが回を重ねて「怪しい」情報の扱いを変えているか（学習しているか）を観察する。

> これは、以前に指摘した2つの穴——「的中率が体感できないと全部ただのランダムに見える」「雰囲気ワードが翻訳表なしでは飾りになる」——を、**数字で検知するための装置**。UIに数字を出さない代わり、開発者は数字で握る。

---

## 13. 受け入れ条件（v0.1 の完成判定）

以下がすべて満たされれば v0.1 完成とみなす。

1. ブラウザ（またはCLI）で、1潜行が **開始→脱出/死亡** までターンループとして回る。
2. マップが3層で描画され、**踏破・視界は決して外れない**。
3. 古地図・噂が「同じ性格・別インスタンス」から生成され、劣化度に応じた信頼度ラベルを持つ。
4. 情報のラベルと実的中率が、§6.2 / §7 の帯に**計測上おおむね収まっている**（計測フックで確認）。
5. 危険度ラベルがプレイヤー状態を反映して算出され、**数字は一切表示しない**。
6. 意味ある行動ごとに **行動前の判断材料 → 行動後の結果対応** が提示される。
7. 死亡時に**判断文脈を含む死亡ログ**が残る。
8. 共有・ランキング・LLM・PWAが**なくても**上記が成立している。
9. 乱数は単一のシード付きPRNG経由で、`runSeed` を固定すれば潜行が**再現可能**。

---

## 14. 非目標（v0.1では作らない）

明示的に後回し。エージェントはこれらを**先回りで実装しない**こと。

- 地図/性格の実プレイヤー間共有、QR、共有リンク
- ランキング、名迷宮アーカイブ、生還率、称号
- LLMによる描写・噂・助言の生成（枠だけ確保、実装は後）
- iPhoneネイティブ化、PWA化、課金
- 完全攻略Wiki的な確定情報、敵HPの数値表示、全体マップの先読み表示
- 10階以上の大規模ダンジョン、複雑なアイテム鑑定ツリー

---

## 15. データモデル（スキーマ骨子）

実装言語に依存しない型スケッチ。命名は英語識別子＋日本語コメントで統一。

```ts
// 迷宮の性格（共有・記憶される単位）
type DungeonCharacter = {
  id: string;
  name: string;                 // 例: "沈黙の井戸"
  biases: {
    metallicEnemyRate: number;  // 金属系の出やすさ 0..1
    upperTrapRate: number;      // 上層の毒罠
    lowerReturnDifficulty: number; // 下層の帰還困難さ
    potionInstability: number;  // 薬効の不安定さ
    rewardWeaponBias: number;   // 報酬の武器寄り度
    enemyDensity: number;       // 敵の少なさ（低いほど少数精鋭）
    enemyLethality: number;     // 一体あたりの危険度
  };
};

type RNG = { next(): number };  // 0..1、注入される単一PRNG

// 具体インスタンス（runSeedで決まる。潜行中は不変）
type DungeonInstance = {
  character: DungeonCharacter;
  runSeed: number;
  floors: Floor[];
};

type Tile =
  | { kind: 'wall' }
  | { kind: 'floor'; explored: boolean }
  | { kind: 'boundary' };       // '?' 判断を迫る境界

type Floor = {
  depth: number;
  grid: Tile[][];
  entities: Entity[];
  items: Item[];
  features: Feature[];          // 泉・崩落・罠など
};

// 情報片：内部確率を持ち、ラベルは射影で得る
type Info = {
  source: InfoSource;           // 'traversed'|'sight'|'sense'|'oldMap'|'survivorNote'|'deathNote'|'ownLog'|'aiHint'
  internalP: number;            // 0..1（UIに出さない）
  claim: Claim;                 // 「南東に泉」など
};
// label(info) => §6.2の帯でラベル文字列を返す

type DangerAssessment = {
  internalRisk: number;         // 0..1 死亡/重傷（UIに出さない）
  // label => §7の帯
};

type PlayerState = {
  condition: number;            // HP相当（言葉で表示）
  hunger: number;
  armorWear: number;
  torch: number;
  inventory: Item[];
  ownLog: Info[];               // 過去の自分の記録（p=0.70..0.90）
};

// 情報解決の結果
type Resolution =
  | { held: true; rendered: Claim }
  | { held: false; missPattern: MissPattern; rendered: Claim };
```

---

## 16. 実装順（エージェントへの推奨）

1. `rng.ts`（シード付きPRNG）→ 再現性の土台。
2. `character.ts` + `generate.ts` → 性格から3〜5階のインスタンス生成。
3. `state.ts` + `ui/render.ts` → 踏破層マップ＋状態表示だけで歩けるループ（この時点では情報は全部“確か”）。
4. `confidence.ts` + `danger.ts` → ラベル射影。
5. `hearsay.ts` + `resolve.ts` → 古地図・噂と、当否解決＋外れ方。
6. §10の行動前後対応と死亡ログ。
7. `telemetry.ts` → 計測フック。
8. §13の受け入れ条件でセルフチェック。

---

*本仕様の一文コンセプト:*
**「これは、マップを埋めるゲームではない。不完全な地図と曖昧な記録を頼りに、未知を信じるか疑うかを、自分の責任で選ぶテキストローグである。」**
